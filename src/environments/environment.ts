// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
	production: false,
	firebaseConfig:
	{
		apiKey: "AIzaSyCZg7lmneJ-3agReM_ElU6XGf1qqzmXFNM",
		authDomain: "angular-image-gallery-900db.firebaseapp.com",
		databaseURL: "https://angular-image-gallery-900db.firebaseio.com",
		projectId: "angular-image-gallery-900db",
		storageBucket: "angular-image-gallery-900db.appspot.com",
		messagingSenderId: "849966186513",
		appId: "1:849966186513:web:4b1ca2bce352611c"
	}
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
